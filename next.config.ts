import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['example.com', 'picsum.photos', 'evhjogolxocysfqkhzlz.supabase.co'], // Add the domains of your external images here
  },
};

export default nextConfig;
